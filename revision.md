你是一位资深机器学习工程师和科学计算开发者。请使用 **superpowers** 技能对整个任务进行规划、拆解和执行管理，过程中允许按需自主调用其他 skills（如文件编辑、代码执行、文档生成等）。

## 指导原则
- 所有修改必须基于 **之前生成的模型分析报告和修改方法的讨论**（`model_analysis_report.md`和'methods_explanation.md'）中指出的问题和建议。
- 仅修改与本次要求相关的部分，项目其余已有功能应保持不变。
- 修改代码后必须进行简短的完整性检查（例如导入测试、参数打印），确保逻辑正确。
- 所有新增或修改的代码必须有清晰的英文注释，说明原因和用法。

---

## 步骤 1：任务规划与环境理解
1. 使用 `superpowers` 将以下步骤拆解为子任务，并管理执行顺序。
2. 通读当前目录下的`model_analysis_report.md`和'methods_explanation.md'，理解之前分析得出的结论、推荐改进项。
3. 定位项目中的核心脚本或模块，理清模型训练、特征筛选、交叉验证、结果输出的代码逻辑。

---

## 步骤 2：修改特征重要性分析与筛选流程
修改特征筛选部分的代码逻辑，满足：
- **特征数 ≥ 10**：保持原有的筛选方法（基于 SHAP 重要性迭代移除最不重要特征）。
- **特征数 < 10**：切换为 **严格的 SHAP-RFECV**（按照之前讨论的那样）并且同样详细输出每次迭代的过程和结果（如筛除特征、R2、MAE等等）

---

## 步骤 3：修改模型超参数优化方案
1. **SVR 模型**：修改SVR的epsilon
2. **Ridge / Lasso 模型**：修改Ridge/Lasso 的 α 参数（使用 `RidgeCV` / `LassoCV` 自动通过 CV 选择最优 α）
3. 保持其他模型（如 RandomForest, XGBoost 等）的超参数不变，除非报告中有明确建议或者你再检测之后认为有修改必要，请你告知我等我来决策。

---

## 步骤 4：升级交叉验证方案
将单一交叉验证升级为双层验证体系：
- **主要评估方法**：**5×5 RepeatedKFold**
- **辅助评估方法**：**LOOCV**（留一法）。
- **输出要求**：在 **每个模型的每一次迭代**（包括特征筛选过程中的每一轮，以及最终模型评估）中，都必须同步计算并输出：
  - 5×5 RepeatedKFold 的 R² 和 MAE
  - LOOCV 的 R² 和 MAE
- **决策依据**：模型性能对比、特征子集选择、最佳模型判定，全部 **主要依据 5×5 RepeatedKFold 的结果**。LOOCV 结果仅作为稳健性参考。
- 每步迭代输出表格对比普通的R2和MAE以及5×5 RepeatedKFold 的 R² 和 MAE和LOOCV 的 R² 和 MAE以及相关数据

---

## 步骤 5：添加 y-randomization（目标变量随机化检验）
- **插入位置**：在通过特征筛选得到每个模型的 **最佳特征数量** 并训练出最佳模型后，立即对该最佳模型执行 y-randomization。
- **方法**：
  1. 保持特征矩阵不变。
  2. 随机打乱目标变量 `y`，破坏特征与目标之间的真实关系。
  3. 使用与最佳模型完全相同的超参数和特征子集，在打乱后的数据上重新进行 **5×5 RepeatedKFold** 交叉验证。
  4. 记录随机化模型在所有重复和折上的 R²、MAE 的均值与分布。
  5. 将真实模型的性能与随机化模型的性能进行对比（可生成直方图或累计分布图）。
- **输出**：在控制台和最终报告中明确显示原模型与随机化模型的性能对比，并判断模型是否具有真正的预测能力。

---

## 步骤 6：生成修改说明文档
创建一个新的 Markdown 文件：**`modifications_explained.md`**，内容应包含：
- **修改总览**：简述本次所做的全部变更。
- **逐项说明**：针对步骤 2~5 中的每一项修改：
  - 修改的具体位置（哪个文件、哪个函数/段落）
  - 修改的目的和作用（为什么要这样改）
  - 新功能的**使用方法**（如果涉及配置参数，说明如何设置；如果新增了函数，提供调用示例）
- **输出结果解读**：指导用户如何阅读新输出的 5×5 RepeatedKFold + LOOCV 结果、RFECV 过程、y-randomization 图表等。
- **注意事项**：可能影响运行时间、依赖包版本等提醒。

---

## 步骤 7：更新 README 与 Pipeline
1. 读取项目根目录下的 `README.md`和`README_CN.md`，更新其中的：
   - 项目工作流描述，反映出新的交叉验证方案和 y-randomization 步骤。
   - 评估指标说明，补充 5×5 RepeatedKFold 为主要指标。
   - 特征筛选策略（大于等于10特征用原方法，小于10用 RFECV）。
   - 快速开始或配置部分的必要变更。
2. 读取 `pipeline.md`，在对应阶段（通常是“模型训练与评估”和“特征筛选”）中插入新的步骤描述，明确写出：
   - 何时使用 RFECV
   - 何时执行 y-randomization
   - 需要记录哪些新的中间结果
3. 保证两个文档的更新部分清晰、准确，与代码实际行为一致。

---

## 步骤 8：最终验证与汇总
1. 再次运行修改后的代码，或至少对关键模块进行静态检查，确保无语法错误、导入问题。
2. 将修改过程中发现的所有疑问、未解决的问题写入 `modifications_explained.md` 的末尾，供后续人工审核。
3. 提交所有更改到的文件列表（由 superpowers 总结）。

请严格按照以上步骤执行，每一步的输出均需清晰可查，最终交付物包括：
- 经修改的源代码文件
- `modifications_explained.md`
- 更新后的 `README.md` 和`README_CN.md`和 `pipeline.md`
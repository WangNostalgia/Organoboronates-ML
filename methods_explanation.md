# 方法论详解：机器学习模型改进策略

> 本文档详细解释了 `model_analysis_report.md` 第6.2和6.3节中提到的各项改进方法，涵盖原理、实现做法和适用场景。

---

## 目录

1. [修改特征选择策略](#1-修改特征选择策略)
2. [集成方法](#2-集成方法)
3. [GPR 不确定性量化](#3-gpr-不确定性量化)
4. [正则化调整](#4-正则化调整)
5. [外部验证](#5-外部验证)
6. [重复 k-fold 交叉验证](#6-重复-k-fold-交叉验证)
7. [y-randomization (目标变量随机化检验)](#7-y-randomization-目标变量随机化检验)

---

## 1. 修改特征选择策略

### 1.1 问题诊断

当前的特征选择策略 `src/feature_selection.py` 使用**固定的绝对阈值**：

- 高相关阈值：|r| > 0.8
- 低重要性阈值：< 20%

问题在于：这些阈值是**经验性**的且**不随数据集变化**。在小样本（~88个）场景下，特征重要性估计本身的方差较大，20%的阈值可能过于严格，导致有信息量的特征被误删。

### 1.2 改进方案：自适应阈值 + 基于CV性能的前向/后向选择

#### 方案A：自适应重要性阈值

**原理**：使用随机排列检验（Permutation Test）确定每个特征的"偶然重要性"基线。对于每个特征，随机打乱其值 N 次，计算每次的重要性，得到一个零分布。如果特征的真实重要性低于零分布的95分位数，则认为该特征不显著，可以考虑移除。

```
步骤：
1. 计算所有特征的原始 SHAP 重要性
2. for each feature:
      for i in range(100):
          随机打乱该特征的值
          重新计算重要性
      记录打乱后的重要性分布
3. 计算每个特征的 p-value = (打乱重要性 > 原始重要性) 的比例
4. 移除 p-value > 0.05 的特征
```

**优势**：阈值随数据自适应，不需要人工设定20%的界限。

#### 方案B：基于 CV 性能变化的选择（推荐）

**原理**：不依赖重要性数值的绝对值，而是直接衡量"移除某个特征后模型 CV 性能的变化"。每次移除候选特征后重新做 CV，只有当 CV 性能不下降（或下降在可接受范围内）时才正式移除。

```
步骤：
1. 当前特征集为 F，当前 CV MAE 为 baseline_mae
2. 按重要性排序所有特征（从低到高）
3. for each 候选特征 f_candidate（从最不重要开始）:
       F' = F - {f_candidate}
       在 F' 上做 CV，计算 candidate_mae
       if candidate_mae <= baseline_mae * 1.05:  # MAE 上升不超过5%
           确认移除 f_candidate
           baseline_mae = candidate_mae
           F = F'
       else:
           保留 f_candidate，继续检查下一个
4. 重复直到无特征可移除
```

**优势**：以性能保护为导向，避免"为了删除而删除"。

#### 方案C：递归特征消除 + 交叉验证 (RFECV)

**原理**：这是 scikit-learn 内置的方法 (`sklearn.feature_selection.RFECV`)。它从全部特征开始，逐步移除最不重要的特征，每一步用 CV 评估性能，最终选择使 CV 性能最优的特征子集。

```
步骤：
1. 初始化模型
2. 在当前特征集上做 CV，记录平均性能
3. 移除重要性最低的特征
4. 重复步骤2-3，直到只剩1个特征
5. 选择 CV 性能最高时对应的特征数量
```

**与当前方法的区别**：RFECV 会自动选择最优的特征数量，而非人为设定 min_features。

### 1.4 三种方案对比

| 方案 | 复杂度 | 计算开销 | 理论基础 | 推荐度 |
|---|---|---|---|---|
| A: 自适应阈值 | 中 | 中（需N次重计算） | 排列检验 | ★★★ |
| B: CV性能导向 | 低 | 高（每次候选都做CV） | 经验风险最小化 | ★★★★★ |
| C: RFECV | 低 | 高（全路径CV） | 经验风险最小化 | ★★★★ |

---

## 2. 集成方法

### 2.1 原理

集成学习（Ensemble Learning）通过组合多个模型的预测来获得比任何单一模型更优的泛化性能。其理论基础是**偏差-方差分解**：

$$\text{Error} = \text{Bias}^2 + \text{Variance} + \text{Irreducible Error}$$

集成方法通过平均降低方差（Variance），而不显著增加偏差（Bias）。

### 2.2 简单平均集成 (Simple Averaging)

**做法**：对多个模型的预测值取算术平均。

$$\hat{y}_{\text{ensemble}} = \frac{1}{M} \sum_{i=1}^{M} \hat{y}_i$$

其中 $M$ 为模型数量，$\hat{y}_i$ 为第 $i$ 个模型的预测值。

**适用场景**：各模型性能相近时效果最好。如果有一个模型明显差于其他，它会拖累整体表现。

### 2.3 加权平均集成 (Weighted Averaging)

**做法**：根据每个模型在验证集上的性能赋予不同权重。

$$w_i = \frac{1 / \text{MSE}_i}{\sum_{j=1}^{M} 1 / \text{MSE}_j}$$

$$\hat{y}_{\text{ensemble}} = \sum_{i=1}^{M} w_i \cdot \hat{y}_i$$

其中权重 $w_i$ 基于模型在 OOS（Out-Of-Sample）数据上的 MSE 的倒数。预测更准确的模型获得更大的话语权。

**在本项目中的应用**（如 `prediction_round2.ipynb` 所示）：

1. 对同一个 SVR 模型，使用 100 个不同的 random_state 进行 100 次不同的 train/test 划分
2. 每次训练一个模型，在验证集上预测
3. 计算每个模型的 MSE（在测试集上），以 1/MSE 作为权重
4. 100 个模型的加权平均作为最终预测

**最佳实践**：使用不同**模型类型**（如 GPR + KRR + Ridge）而非同一模型的不同划分进行集成，因为不同类型的模型捕捉不同的数据模式，集成效果更好。

### 2.4 Stacking (堆叠泛化)

**做法**：训练一个"元模型"（meta-model）来学习如何最优地组合基模型的预测。

```
步骤：
1. 将训练集分为 K 折
2. 对每个基模型，用 K-1 折训练，对留出的1折预测
      → 得到每个基模型对所有训练样本的 "out-of-fold" 预测
3. 将所有基模型的 out-of-fold 预测作为新特征 X_meta
4. 以原始目标 y 训练元模型（如 Ridge）
5. 预测时：基模型预测 → 元模型组合 → 最终预测
```

**优势**：元模型可以学习非线性的组合方式，比简单/加权平均更灵活。

### 2.5 推荐的集成组合

基于本次训练结果，推荐以下集成方案：

| 集成方案 | 基模型 | 方式 | 预期 R²_LOO |
|---|---|---|---|
| 2-model avg | GPR + KRR | 简单平均 | ~0.82 |
| 3-model avg | GPR + KRR + Ridge | 简单平均 | ~0.81 |
| 3-model weighted | GPR + KRR + Ridge | 加权平均 | ~0.82 |
| Stacking | 全部14模型 | Ridge元模型 | 视数据而定 |

---

## 3. GPR 不确定性量化

### 3.1 原理

高斯过程回归 (GPR) 的核心优势是它不仅能预测目标值，还能**同时输出预测的不确定性（方差）**。这是 GPR 区别于其他回归模型的根本特征。

GPR 假设函数值服从多元高斯分布：

$$f(\mathbf{x}) \sim \mathcal{GP}(m(\mathbf{x}), k(\mathbf{x}, \mathbf{x}'))$$

其中 $m(\mathbf{x})$ 是均值函数（通常是常数或零），$k(\mathbf{x}, \mathbf{x}')$ 是核函数（协方差函数）。

对新输入点 $\mathbf{x}_*$，预测分布为：

$$\mu_* = \mathbf{k}_*^T (\mathbf{K} + \sigma_n^2 \mathbf{I})^{-1} \mathbf{y}$$

$$\sigma_*^2 = k(\mathbf{x}_*, \mathbf{x}_*) - \mathbf{k}_*^T (\mathbf{K} + \sigma_n^2 \mathbf{I})^{-1} \mathbf{k}_*$$

其中 $\mu_*$ 是预测均值，$\sigma_*^2$ 是预测方差。

### 3.2 在本项目中的应用

**意义**：活化能预测的不确定性量化可以直接指导后续工作：

1. **高不确定性区域** → 优先进行 DFT 计算或实验验证
2. **低不确定性区域** → 可以信任模型预测，减少计算资源

**实践做法**：

```python
from sklearn.gaussian_process import GaussianProcessRegressor
from sklearn.gaussian_process.kernels import RBF, WhiteKernel

# 1. 定义核函数
kernel = RBF(length_scale=1.0) + WhiteKernel(noise_level=0.1)

# 2. 训练 GPR（return_std=True 时返回预测标准差）
gpr = GaussianProcessRegressor(kernel=kernel, alpha=1e-5, random_state=42)
gpr.fit(X_train_scaled, y_train_scaled)

# 3. 预测 + 获取不确定性
y_pred, y_std = gpr.predict(X_test_scaled, return_std=True)

# 4. 95% 置信区间
y_lower = y_pred - 1.96 * y_std
y_upper = y_pred + 1.96 * y_std

# 5. 识别高不确定性样本
high_uncertainty_mask = y_std > np.percentile(y_std, 80)
priority_samples = X_test[high_uncertainty_mask]  # 优先进行 DFT 验证
```

**可视化**：预测 vs 实际散点图中，使用误差棒（error bars）标注每个预测的 ±1.96σ 置信区间。

---

## 4. 正则化调整

### 4.1 SVR 的 ε 参数

**原理**：SVR 的 ε 参数定义了"不敏感区间"——预测值与真实值之差小于 ε 时不产生任何损失。ε 越大，模型越"宽容"，支持向量越少，模型越简单。

$$\text{Loss} = \max(0, |y - \hat{y}| - \varepsilon)$$

**当前问题**：本次训练的 SVR 最优 ε = 1.41 kcal/mol。这意味着模型对 1.41 kcal/mol 以内的误差完全"不在意"。这个值在活化能预测中偏大——1.41 kcal/mol 已经接近化学精度要求（通常要求 < 1 kcal/mol）。

**调整建议**：

| ε 范围 | 效果 | 适用场景 |
|---|---|---|
| 0.01 - 0.1 | 极严格拟合，可能过拟合 | 数据噪声极低时 |
| 0.1 - 0.5 | 适中，平衡拟合与泛化 | **推荐用于活化能预测** |
| 0.5 - 2.0 | 宽松，强正则化 | 数据噪声较大时 |

### 4.2 Ridge/Lasso 的 α 参数

**原理**：

- Ridge (L2 正则化)：$\text{Loss} = \text{MSE} + \alpha \sum w_i^2$。收缩所有系数但不为零。
- Lasso (L1 正则化)：$\text{Loss} = \text{MSE} + \alpha \sum |w_i|$。可能将部分系数压缩为零，实现自动特征选择。

**α 选择策略**：

1. 使用 `RidgeCV` / `LassoCV` 自动通过 CV 选择最优 α
2. 绘制 α 路径图（coefficient paths），观察各特征的系数随 α 的变化
3. 选择使 CV 误差最小且模型不过度简化的 α

### 4.3 实践指导

```python
# SVR: 将 epsilon 搜索范围下移
# optuna 参数空间修改为:
epsilon = trial.suggest_float("epsilon", 1e-3, 0.5, log=True)

# RidgeCV 自动选择 alpha
from sklearn.linear_model import RidgeCV
ridge = RidgeCV(alphas=np.logspace(-3, 3, 50), cv=LeaveOneOut())
ridge.fit(X_train, y_train)
print(f"最优 alpha: {ridge.alpha_}")
```

---

## 5. 外部验证

### 5.1 原理与必要性

**内部验证**（如 train/test split、LOOCV）评估的是模型在**与训练数据同分布**的数据上的性能。但 ML 模型的真正价值在于对**全新反应体系**的预测能力。

**外部验证**：使用完全独立于训练数据的新数据集测试模型。在化学 ML 中，外部验证数据通常是：

- 新的底物组合（不在训练集的 sub_H × sub_B 空间中）
- 来自不同实验室或计算方法的数据
- 时间上后续收集的数据

### 5.2 本项目中的外部验证实现

`src/validation_process.py` 设计用于从已有的 sub_H 和 sub_B 组分中生成**全部可能的组合**。这些组合中，部分存在于训练数据中（有已知活化能），部分是完全新的配对（活化能未知）。

```
训练数据中的组合:     完整组合空间:
sub_H × sub_B          sub_H × sub_B
┌───┬───┬───┐         ┌───┬───┬───┬───┬───┐
│ ■ │   │ ■ │         │ ■ │ · │ ■ │ · │ · │
├───┼───┼───┤         ├───┼───┼───┼───┼───┤
│   │ ■ │   │    →    │ · │ ■ │ · │ · │ · │
├───┼───┼───┤         ├───┼───┼───┼───┼───┤
│ ■ │   │   │         │ ■ │ · │ · │ · │ · │
└───┴───┴───┘         └───┴───┴───┴───┴───┘
  ■ = 已知组合          ■ = 已知, · = 新组合（需预测）
```

**外部验证的正确做法**：

1. **时间分割**：按反应发现的时间顺序划分训练/验证
2. **结构分割**：按化学骨架划分（如不同取代基类型的底物分到不同集合）
3. **组合空间外推**：使用 `validation_process.py` 生成所有组合，然后预留部分 sub_H 的全部组合

**评估指标**：

- MAE_ext：外部验证集的平均绝对误差
- Q²_ext：外部验证的 Q²（类似 R² 但使用训练集的均值作为基线）
- Coverage：预测区间覆盖真实值的比例（对 GPR 的不确定性校准尤为重要）

---

## 6. 重复 k-fold 交叉验证

### 6.1 原理

**单次 k-fold CV 的问题**：k-fold CV 的性能估计依赖于一次特定的数据划分。虽然比单次 train/test split 更稳定，但仍然存在因划分方式引入的方差。

**重复 k-fold CV (Repeated k-fold)**：将 k-fold CV 重复 R 次，每次使用不同的随机种子进行数据划分。最终的 CV 指标是 R × k 次评估的平均值。

$$\text{MAE}_{\text{RepeatedKFold}} = \frac{1}{R \times K} \sum_{r=1}^{R} \sum_{k=1}^{K} \text{MAE}_{r,k}$$

### 6.2 LOOCV vs Repeated 5-fold 对比

| 方法 | 折数 | 训练/验证划分方式 | 方差 | 偏差 | 计算开销 |
|---|---|---|---|---|---|
| LOOCV | N | 每个样本验证一次 | **高** | **低** | O(N²) |
| 5-fold CV | 5 | 每折20%验证 | 中 | 中 | O(5) |
| 5×5 Repeated | 25 | 5折×5次不同划分 | **低** | 中 | O(25) |
| 10×10 Repeated | 100 | 10折×10次不同划分 | **极低** | 中 | O(100) |

**LOOCV 的陷阱**：在 N=88 的小样本下，LOOCV 的方差可能非常大（单次训练仅用 N-1=87 个样本，任何异常样本都会严重影响评估）。对 GPR 和 SVR 观察到的 R²_LOO 与 R²_Test 巨大差异可能源于此。

**推荐**：使用 5×5 RepeatedKFold 作为主要评估方法，LOOCV 作为辅助。

### 6.3 scikit-learn 实现

```python
from sklearn.model_selection import RepeatedKFold, cross_val_score

# 5折重复5次 = 25次评估
rkf = RepeatedKFold(n_splits=5, n_repeats=5, random_state=42)
scores = cross_val_score(model, X_scaled, y, cv=rkf, scoring='neg_mean_absolute_error')
print(f"Repeated 5-fold MAE: {-scores.mean():.4f} ± {scores.std():.4f}")
```

给出的标准差 `scores.std()` 是模型稳定性衡量的重要补充指标。

---

## 7. y-randomization（目标变量随机化检验）

### 7.1 原理

**核心问题**：模型的好性能是因为学到了真实的构效关系，还是因为特征空间与目标变量的某种偶然相关性？

**y-randomization 回答这个问题**：

1. 将目标变量 y 随机打乱（破坏特征与目标之间的真实关系）
2. 在打乱后的数据上重新训练模型
3. 比较原始模型的性能与打乱后模型的性能
4. 如果原始模型显著优于所有打乱模型 → 模型学到了真实关系
5. 如果原始模型与打乱模型性能接近 → 模型的性能可能来自偶然

### 7.2 统计检验

重复 y-randomization N 次（如 100 次），得到 N 个随机模型的性能指标分布。计算：

$$p = \frac{\text{随机模型中性能}\ ≥\ \text{原始模型性能的次数}}{N}$$

如果 p < 0.05（即原始模型性能显著高于随机基线），则模型通过了随机化检验。

### 7.3 评判标准

| 场景 | 解释 | 行动 |
|---|---|---|
| 原始 R² 显著高于随机分布 | 模型学到了真实的构效关系 | 模型可信 |
| 原始 R² 在随机分布范围内 | 可能存在偶然相关性 | 需要更多数据或更严格验证 |
| 原始 R² 低于随机分布均值 | 模型未学到关系 | 重新检查数据和特征工程 |

### 7.4 实践步骤

```
for i in range(100):
    y_shuffled = np.random.permutation(y)        # 打乱目标变量
    model.fit(X, y_shuffled)                     # 在破坏的数据上训练
    r2_random[i] = CV_score(model, X, y_shuffled) # 记录性能

# 绘制分布
plt.hist(r2_random, bins=20)
plt.axvline(r2_original, color='red', label=f'Original R²={r2_original:.3f}')

# 计算 p-value
p_value = np.mean(r2_random >= r2_original)
```

---

---

## 深入探讨：三个关键方法论问题

> 本节针对前述改进方法中的三个核心问题进行深入解析。

---

### 问题一：SHAP-RFECV 是否比标准 RFECV 更好？

#### 1.1 标准 RFECV 的工作原理

标准 RFECV 使用模型的**内置特征重要性**（如 `feature_importances_` 或 `coef_`）来决定每次移除哪个特征。这对树模型和线性模型效果好，但存在以下局限：

| 模型类型 | 内置重要性来源 | 局限性 |
|---|---|---|
| SVR (RBF核) | 不支持内置重要性 | RFECV 对 SVR 不可直接使用 |
| KRR | 不支持内置重要性 | 同上 |
| GPR | 不支持内置重要性 | 同上 |
| MLP | 基于权重的启发式 | 对非线性关系捕捉不完整 |
| KNR | 不支持内置重要性 | 不可直接使用 |

**本项目中的问题**：最佳模型 GPR、SVR、KRR 都**不支持内置特征重要性**。标准 RFECV 对它们根本不可用。

#### 1.2 SHAP-RFECV 的设计思路

将 SHAP 嵌入 RFECV 的每一步：

```
SHAP-RFECV 算法流程:
┌─────────────────────────────────────────────────────────────┐
│  初始特征集 F = {全部15个特征}                               │
│                                                              │
│  while len(F) >= min_features:                               │
│    ┌──────────────────────────────────────────────────┐     │
│    │ 步骤A: 训练模型（当前特征集 F）                    │     │
│    │ 步骤B: 使用 SHAP 计算每个特征的重要性              │     │
│    │        (KernelExplainer / TreeExplainer 自动匹配)  │     │
│    │ 步骤C: 移除 SHAP 重要性最低的 1 个特征             │     │
│    │ 步骤D: 用 CV 评估当前特征集的性能                  │     │
│    │ 步骤E: 记录 (特征数, CV性能, 被移除特征)           │     │
│    └──────────────────────────────────────────────────┘     │
│                                                              │
│  选择 CV 性能最优时对应的特征数 → 最终特征集                 │
└─────────────────────────────────────────────────────────────┘
```

#### 1.3 SHAP-RFECV vs 标准 RFECV vs 当前方法 对比

| 维度 | 当前方法 (固定阈值) | 标准 RFECV | SHAP-RFECV |
|---|---|---|---|
| **适用范围** | 所有模型 ✓ | 仅支持内置重要性的模型 ✗ | **所有模型 ✓** |
| **重要性来源** | SHAP（模型无关） | 模型内置（模型相关） | **SHAP（模型无关）** |
| **停止准则** | 固定阈值 (<20%) + min_features | CV 性能拐点 | **CV 性能拐点** |
| **特征选择路径** | 贪心（移除即不可逆） | 贪心 | 贪心 |
| **计算开销** | 低（每轮1次SHAP） | 低（无需SHAP） | **高（每轮1次完整SHAP）** |
| **能否发现最优特征数** | ✗（预设 min_features） | ✓ | ✓ |

#### 1.4 SHAP-RFECV 的理论优势

**优势1：模型无关性**

SHAP 是模型无关的解释方法。同一个 SHAP-RFECV 框架可以一致地应用于 SVR、KRR、GPR、RandomForest、XGBoost 等所有模型类型，无需为每种模型单独实现特征重要性计算。这解决了本项目核心模型（GPR/SVR/KRR）无法使用标准 RFECV 的问题。

**优势2：更准确的重要性归因**

模型内置的重要性（如 permutation importance 或 coef_）在某些情况下会给出误导性结果：
- 当特征之间存在高度相关时（如本项目中的 VBur_C 与 NPA_charge_B 相关 0.85），内置重要性可能在两个共线特征之间任意分配
- SHAP 基于 Shapley 值，在博弈论框架下保证公平分配——每个特征的贡献被公平地衡量，考虑所有可能的特征组合

**优势3：数据驱动的停止准则**

当前方法人为设定 min_features=3（或5、8），这是一个任意的外部决定。SHAP-RFECV 通过 CV 性能曲线的拐点自动确定最优特征数——这是数据驱动的、模型特定的最优选择。

```
CV MAE
  │
  │  ██
  │  ████
  │  ██████
  │  ████████  ← 更多特征 → 性能改善
  │  ████████████████████  ← 平台期（最优区域）
  │  ████████████████████████  ← 冗余特征 → 性能不再改善
  │  ███████████████████████████
  └──────────────────────────────→ 特征数量
    15  12  9   7   5   3   1
  
  最优特征数 = 平台期起点（如 7-9 个特征）
```

#### 1.5 SHAP-RFECV 的代价

**计算开销显著增大**。以本项目为例：
- 当前方法：每模型每迭代1次 SHAP（KernelExplainer），共 12 次 × 14 模型 = 168 次 SHAP 调用
- SHAP-RFECV：每模型每特征1次 SHAP，共 15+14+13+...+4 = 114 次 SHAP 调用（单个模型从15个特征降到3个）。而 KernelExplainer 每次调用可能需要数分钟。

**缓解策略**：
1. **仅对最终候选模型使用 SHAP-RFECV**（如只用 GPR/SVR/KRR 三个），而非所有14个模型
2. **使用 TreeExplainer 加速**（对 RandomForest/XGBoost 等树模型，SHAP 计算极快）
3. **增大 K-means 聚类数但减少 SHAP 采样数**，在不显著损失准确性的前提下加速 KernelExplainer

#### 1.6 结论：推荐使用，但需要策略性应用

SHAP-RFECV 确实优于标准 RFECV 和当前固定阈值方法，尤其适用于本项目（核心模型不支持内置重要性）。推荐采用**两阶段策略**：

```
第一阶段（快速筛选）: 使用当前迭代方法，快速缩小特征范围（15 → ~8）
第二阶段（精细选择）: 对候选模型使用 SHAP-RFECV，在 8→3 范围内确定最优特征数
```

---

### 问题二：集成学习的模型组合应该如何确定？

#### 2.1 核心原则：多样性 + 准确性

集成学习有效的根本原因是**误差的不相关性**。如果两个模型犯完全相同的错误，集成它们没有收益。因此，选择集成模型成员的核心标准是：

**好成员 = 自身准确 + 互相不同**

更形式化地，集成模型的泛化误差可以分解为：

$$E_{\text{ensemble}} = \bar{E} - \bar{A}$$

其中 $\bar{E}$ 是基模型的平均误差，$\bar{A}$ 是基模型之间的**分歧度（Ambiguity）**——即它们的预测差异程度。分歧度越大，集成效果越好。

#### 2.2 模型选择的具体流程

**不应该**随意选几个表现好的模型直接集成。**应该**按照以下步骤系统性地选择：

```
步骤1: 候选模型池建立
├── 从所有训练模型中，筛选出 R²_LOO > 阈值（如0.70）的模型
├── 排除明显欠拟合的模型（如 DecisionTree R²=0.37）
└── 得到候选池（本项目: GPR, SVR, KRR, GB, AdaBoost, XGBoost, Ridge, LR, Lasso, ElasticNet, KNR）

步骤2: 误差相关性分析
├── 对候选池中每对模型 (i, j)
│   ├── 在验证集上计算两个模型的预测残差
│   ├── res_i = y_true - y_pred_i
│   ├── res_j = y_true - y_pred_j
│   └── 计算残差的 Pearson 相关系数 r_res(i, j)
└── 生成误差相关矩阵

步骤3: 聚类分组
├── 对误差相关矩阵进行层次聚类
├── 相关性高的模型 → 同一组（它们犯类似的错误）
├── 相关性低的模型 → 不同组（它们互补）
└── 可视化：热力图 + 树状图

步骤4: 从不同组中选择代表
├── 每组选一个性能最好的模型作为代表
└── 代表集合 → 集成候选

步骤5: 穷举组合评估
├── 对 {2, 3, 4, ...} 个模型的组合
│   ├── 穷举所有可能的组合
│   ├── 对每个组合做 5-fold CV
│   └── 记录 CV MAE
└── 选择 CV MAE 最低的组合
```

#### 2.3 以本项目为例的实战推演

**步骤1（候选池）**：基于 R²_LOO，排除 DecisionTree (0.707) 和 MLP (0.702)，保留其余12个。

**步骤2（误差相关性）**：这是关键一步。预期发现：
- LinearRegression、Ridge、Lasso 三者残差高度相关（都是线性模型族）
- GPR 与 KRR 残差可能中等相关（都使用 RBF 核）
- SVR 与 KRR 残差可能中等相关（类似核函数）
- 树模型族（RandomForest、GB、XGBoost、AdaBoost）内部残差高度相关

**步骤3-4（分组）**：预期的分组结果：

| 组别 | 成员 | 误差特征 | 组内最佳 |
|---|---|---|---|
| **核方法组** | GPR, SVR, KRR | 对非线性模式敏感，外推能力好 | GPR (R²_LOO=0.811) |
| **线性模型组** | Ridge, LR, Lasso, ElasticNet | 稳定、可解释、外推保守 | Ridge (R²_LOO=0.755) |
| **树模型组** | RF, GB, XGBoost, AdaBoost | 擅长捕捉交互效应 | GB (R²_LOO=0.782) |
| **实例方法组** | KNR | 非参数、局部拟合 | KNR (R²_LOO=0.739) |

**步骤5（组合评估）**：核心发现——跨组组合优于组内组合。

| 组合 | CV MAE (预测) | 理由 |
|---|---|---|
| GPR + KRR | 2.45 | 同组，残差相关 → 改进有限 |
| GPR + Ridge | 2.30 | 跨组组合 → 互补性好 |
| **GPR + KRR + Ridge** | **2.20** | **三组代表 → 最佳多样性** |
| GPR + KRR + GB | 2.25 | 核方法冗余，树模型补充 |
| 全部5个 | 2.35 | 过多模型引入噪声 |

#### 2.4 关键认知

1. **不是越多越好**：3-5个多样化模型通常效果最佳。加入更多模型后边际收益递减，且可能引入更多噪声。

2. **跨模型族选择**：来自不同模型族的成员（核方法 + 线性模型 + 树模型）比同族多个成员（3种不同的核方法）更好。

3. **必须做误差相关性分析**：只看 R²/MAE 排名就选 top-N 模型集成是常见的错误做法。如果 top-3 模型都犯类似的错误，集成它们毫无意义。

4. **集成权重需在验证集上学习**：不要用训练集的性能来决定权重。应该在一个独立的验证集（可以从训练集中再划出10-15%）上计算每个模型的 MSE 来确定权重。

---

### 问题三：Repeated k-fold vs LOOCV 的角色分工

#### 3.1 两种评估方法的本质区别

| 维度 | LOOCV | 5×5 RepeatedKFold |
|---|---|---|
| **评估次数** | N次（样本数，如88次） | 25次（5折 × 5次重复） |
| **每次训练样本数** | N-1个（如87个） | ~N×0.8个（如~70个） |
| **每次验证样本数** | 1个 | ~N×0.2个（如~18个） |
| **方差来源** | 单个样本的随机性 | 多折划分的随机性 |
| **偏差** | 极低（用了几乎所有数据训练） | 略高（用80%数据训练） |
| **方差** | **高**（88次评估基于88个单样本） | **低**（25次评估基于25组多样本） |
| **计算开销** | 高（N次训练） | 中（25次训练） |

#### 3.2 为什么 LOOCV 的方差反而更高

直觉上 LOOCV"用了更多数据"应该更稳定。但事实恰恰相反：

**LOOCV 高方差的根本原因**：

1. **每次评估只基于1个样本**：一个异常样本（实验误差、DFT计算误差）就会产生一个极端的评估值。在 N=88 的数据集上，LOOCV 的输出是88个"point estimate"的均值，每个 point estimate 都来自仅1个样本。
2. **训练集之间高度重叠**：任意两个 LOOCV 折之间的训练集只差1个样本。这意味着88次评估不是独立的——一个"困难样本"在它被留出时会产生极端的误差，但在其他87次评估中它都在训练集中，影响了整个模型的参数。
3. **在小样本下尤为严重**：当 N < 100 时，单个样本对模型的影响权重很大。LOOCV 对数据中的每一个"特殊样本"都极其敏感。

**RepeatedKFold 低方差的原因**：

1. **每次评估基于多个样本**（~18个）→ 单个异常样本的影响被稀释
2. **不同重复之间的数据划分独立** → 25次评估更具代表性
3. **5折的划分方式让训练集之间有较大差异** → 评估覆盖了更多样的数据子集组合

一个类比：LOOCV 相当于对每个学生单独出题测试（题目只有1道），而 RepeatedKFold 相当于分成5组出题测试（每组有18道题），且重复5次换不同的分组方式。显然 RepeatedKFold 的分数更能反映真实水平。

#### 3.3 "主要评估"与"辅助评估"的具体含义

**RepeatedKFold（主要评估）——用于决策判断：**

- ✅ 模型选择：哪个模型更好 → 看 RepeatedKFold 的 MAE 均值
- ✅ 模型稳定性：模型是否稳定 → 看 RepeatedKFold 的 MAE 标准差
- ✅ 超参数调优：这组超参数是否更好 → 看 RepeatedKFold 的 MAE 变化
- ✅ 特征选择：这个特征是否该保留 → 看 RepeatedKFold 的 MAE 变化
- ✅ 最终报告的性能指标：论文/报告中的模型性能 → 报告 RepeatedKFold 的结果

**LOOCV（辅助评估）——用于补充信息和敏感性检查：**

- 🔍 识别异常样本：LOOCV 中 MAE 异常高的那个样本 → 可能数据有问题
- 🔍 评估单个样本的影响：移除某个样本后模型变化多大 → 数据鲁棒性
- 🔍 与传统 QSAR 文献对比：很多化学信息学文献使用 LOOCV → 便于横向比较
- 🔍 确认 RepeatedKFold 的结论：如果 LOOCV 与 RepeatedKFold 结果一致 → 增加信心；如果不一致 → 需要深入调查原因
- ❌ **不用于**：模型排名、超参数选择、特征选择决策

#### 3.4 两者出现矛盾时怎么办

**场景**：RepeatedKFold 显示模型A优于模型B，但 LOOCV 显示模型B优于模型A。

**原因分析**（以本项目为例）：GPR 的 LOOCV R²=0.811（很高），但 Test R²=0.620（一般）。而 Ridge 的 LOOCV R²=0.755（一般），Test R²=0.676（较高）。

```
         RepeatedKFold (5×5)    LOOCV
GPR:      MAE ~2.50              MAE ~2.29 (看似更优)
Ridge:    MAE ~2.85              MAE ~2.90 (看似更差)
```

如果 LOOCV 判断 GPR 更优，而 RepeatedKFold 判断 Ridge 更优，该信谁？

**决策逻辑**：

1. **首选 RepeatedKFold**。因为它在小样本下的方差更低，评估更稳定。
2. **调查不一致的原因**。GPR 的 LOOCV 与 RepeatedKFold 差异大 → 说明 GPR 对个别训练样本高度敏感，模型不够稳健。这本身就是重要发现——**应该选 Ridge**。
3. **LOOCV 的"矛盾信号"本身是有价值的信息**：它揭示了模型的敏感性。如果一个模型在 LOOCV 和 RepeatedKFold 之间表现差异很大（如本例中的 GPR），说明该模型的预测对训练数据的微小变化不稳定 → 不适合作为生产模型。

#### 3.5 报告中的呈现方式

在最终报告或论文中，建议以如下方式呈现评估结果：

| 模型 | Repeated 5-fold MAE | Repeated 5-fold R² | LOOCV R² |
|---|---|---|---|
| GPR | 2.50 ± 0.31 | 0.770 ± 0.05 | 0.811 |
| KRR | 2.55 ± 0.28 | 0.760 ± 0.04 | 0.783 |
| Ridge | 2.85 ± 0.22 | 0.740 ± 0.03 | 0.755 |

- **主要依据**：RepeatedKFold 的 MAE 均值和标准差（加粗）
- **辅助参考**：LOOCV R²（用于与文献对比）
- **判断标准**：优先看 MAE 均值（越低越好）和标准差（越小越稳定）

---

### 问题四：为什么推荐 5-fold？不同 k 值如何根据数据集大小选择？

#### 4.1 k 值的本质含义

k-fold 交叉验证中，k 决定了**训练集与验证集的比例**：

| k | 训练比例 | 验证比例 | 每次训练的样本数 (N=100时) |
|---|---|---|---|
| 2 | 50% | 50% | 50 |
| 3 | 67% | 33% | 67 |
| **5** | **80%** | **20%** | **80** |
| 10 | 90% | 10% | 90 |
| N (LOOCV) | (N-1)/N ≈ 99% | 1/N ≈ 1% | 99 |

k 值的本质是**偏差-方差权衡**：

$$\text{MSE}_{\text{CV}} = \text{Bias}^2 + \text{Variance}$$

- **k 越小** → 训练集越小 → 模型偏差越大（因为训练数据更少） → 但验证集大 → CV 估计方差小
- **k 越大** → 训练集越大 → 模型偏差越小（接近用全部数据训练） → 但验证集小 → CV 估计方差大

#### 4.2 为什么 5-fold 和 10-fold 是最常用的？

**经验研究与理论分析**（Hastie et al., 2009; Kohavi, 1995）：

20世纪90年代，Ron Kohavi 用大量真实数据集系统比较了不同 k 值的表现，发现：

```
k=2:  偏差过高（只用50%数据训练），CV误差显著大于真实泛化误差
k=5:  偏差-方差平衡良好，CV误差最接近真实泛化误差
k=10: 方差略高于k=5，但与真实泛化误差的差距也很小
k=20: 方差开始明显增大
k=N:  方差最大的情况（LOOCV）
```

**5-fold 被广泛推荐的原因**：
1. 80/20 的划分比例与机器学习中最常用的 train/test split 一致
2. 5次训练的计算开销可控
3. 每次验证集 20%（约18个样本）足够给出稳定的评估
4. 5次评估的均值方差已足够低

**10-fold 也很常用**，尤其在某些领域（如生物信息学）是默认选择。它比5-fold 偏差稍低但方差稍高。

#### 4.3 按数据集大小选择 k 的实用指南

核心原则：**验证集的绝对样本数**比 k 的具体数值更重要。

验证集需要多大？经验法则：

- **回归任务**：每次验证至少需要 **15-20 个样本**，才能给出稳定的 MAE/RMSE 估计
- **分类任务**：每次验证至少需要 **每个类别 5-10 个样本**

由此可以推导不同数据集大小下的推荐 k 值：

| 总样本数 N | 推荐 k | 验证集大小 | 理由 |
|---|---|---|---|
| **N < 30** | LOOCV 或 k=N | 1 | 数据太少，只能用 LOOCV 最大化训练数据 |
| **30 ≤ N < 60** | k=3 或 k=5 | 10-20 | 验证集勉强够，k 不宜大 |
| **60 ≤ N < 150** (本项目 ~88) | **k=5** | **~18** | 验证集 ≈ 18，恰好满足回归任务的稳定评估需求 |
| **150 ≤ N < 500** | k=5 或 k=10 | 15-100 | 两种都可以，差别不大 |
| **500 ≤ N < 2000** | k=10 | 50-200 | 验证集足够大，k=10 偏差更小 |
| **N ≥ 2000** | k=10 或 hold-out | 200+ | 即使 k=10 验证集也很大；也可以直接 hold-out |

#### 4.4 以本项目为例：为什么 k=5 是对的

本项目 N ≈ 88：

```
如果 k=3:  训练 59，验证 29 → 验证够大，但训练只有59个（可能不够）
如果 k=5:  训练 ~70，验证 ~18 → 训练够大，验证 ≥15（满足稳定评估）
如果 k=10: 训练 ~79，验证 ~9  → 验证不够15（MAE估计不够稳定）
如果 k=88 (LOOCV): 训练 87，验证 1 → 训练最大，但验证仅1个（方差极大）
```

**k=5 是 N=88 下的最优选择**：在训练集大小（~70个，足够学习）和验证集大小（~18个，足够稳定估计 MAE）之间取得了平衡。

#### 4.5 直观对照：不同 k 下的评估稳定性

以本项目 SVR 模型模拟（基于实际数据量 N=88）：

```
┌─────────────────────────────────────────────────────────────┐
│  k=2:  训练44 / 验证44                                       │
│        MAE = [2.45, 2.31]                                   │
│        均值=2.38, 范围=0.14  ← 只评估2次，样本不足           │
├─────────────────────────────────────────────────────────────┤
│  k=5:  训练70 / 验证18                                       │
│        MAE = [2.51, 2.33, 2.47, 2.29, 2.55]                 │
│        均值=2.43, 范围=0.26  ← 5次评估，每次验证18个样本     │
├─────────────────────────────────────────────────────────────┤
│  k=10: 训练79 / 验证9                                        │
│        MAE = [2.3, 2.1, 3.0, 2.5, 1.8, 2.9, 2.2, 2.7,      │
│               2.4, 3.1]                                      │
│        均值=2.50, 范围=1.30  ← MAE范围暴增！验证样本太少     │
├─────────────────────────────────────────────────────────────┤
│  k=88: 训练87 / 验证1                                        │
│        (LOOCV)                                               │
│        MAE = [1.2, 0.3, 5.8, 2.1, 0.1, 9.2, ...] (88个值)  │
│        均值=2.29, 范围=9.1   ← 范围极大！每个样本一个极端值  │
└─────────────────────────────────────────────────────────────┘
```

**k=10 时**，某一次验证恰好分到几个异常样本 → MAE 极端（1.8 或 3.1）。在只有 9 个验证样本时，一两个异常值就能把 MAE 拉偏。

**k=5 时**，每次有 18 个验证样本 → 异常值的影响被稀释 → MAE 波动小。

#### 4.6 推荐重复 k-fold 时的 k 和重复次数组合

基于上述分析，给出面向不同数据规模的推荐值：

| 总样本 N | k | 重复次数 | 总评估次数 | 适用场景 |
|---|---|---|---|---|
| N < 60 | 5 | 10 | 50 | 增加评估次数以补偿小样本的不稳定性 |
| **60 ≤ N < 150** | **5** | **5** | **25** | **本项目推荐** |
| 150 ≤ N < 500 | 10 | 5 | 50 | 大样本下 k=10 偏差更低 |
| N ≥ 500 | 10 | 3 | 30 | 样本足够，重复次数可以减少 |

#### 4.7 总结：选择 k 的三步决策法

```
第一步: 计算验证集大小 = N / k
       → 验证集是否 ≥ 15 (回归) 或 ≥ 每类5 (分类)?
       → 如果是: k可用; 如果否: 减小k

第二步: 检查计算开销
       → k次训练是否可以接受?
       → 如果否: 考虑减小k或使用hold-out

第三步: 确定重复次数
       → N < 150: 重复5-10次以降低方差
       → N ≥ 150: 重复3-5次即可
```

**对于本项目 (N=88)**：k=5 是唯一同时满足"验证集 ≥ 15"和"训练集足够大"的选择，配合 5 次重复，总共 25 次评估，可在偏差、方差和计算开销之间取得最优平衡。

---

### 问题五：线性模型参数详解 — 为什么 Lasso/ElasticNet 比 Ridge 多这么多参数？SVR 的 gamma 是否需要调整？

#### 5.1 Ridge 为什么最简单（只有一个 α）？

Ridge 回归使用 **L2 正则化**：

$$\text{Loss} = \underbrace{\|\mathbf{y} - \mathbf{X}\mathbf{w}\|_2^2}_{\text{MSE}} + \underbrace{\alpha \sum_{i=1}^{p} w_i^2}_{\text{L2 惩罚}}$$

**L2 正则化的数学特性决定了它极其稳定**：

1. 损失函数是处处可微的严格凸函数 → 有唯一解析解：$\hat{\mathbf{w}} = (\mathbf{X}^T\mathbf{X} + \alpha\mathbf{I})^{-1}\mathbf{X}^T\mathbf{y}$
2. 所有系数同时被均匀收缩，但**不会被压缩为零** → 不会出现系数突然消失的数值不稳定
3. 不需要 `max_iter`（迭代求解器） — 闭式解一步到位
4. 不需要 `tol`（收敛容差） — 没有迭代过程
5. 不需要 `selection` — 没有变量选择行为

**结论：Ridge 本质上只有 α 一个有意义的需要调节的参数。用 RidgeCV 一把梭就行。**

#### 5.2 Lasso 为什么参数更多？

Lasso 使用 **L1 正则化**：

$$\text{Loss} = \underbrace{\|\mathbf{y} - \mathbf{X}\mathbf{w}\|_2^2}_{\text{MSE}} + \underbrace{\alpha \sum_{i=1}^{p} |w_i|}_{\text{L1 惩罚}}$$

**L1 正则化的数学特性带来了额外的复杂性**：

| 参数 | 用途 | 为什么需要 |
|---|---|---|
| **`alpha`** | 正则化强度 | 同 Ridge，控制惩罚力度 |
| **`max_iter`** | 最大迭代次数 | **L1 无可供的闭式解，必须用坐标下降法迭代求解。** 默认 1000 次有时不够收敛 |
| **`tol`** | 收敛容差 | 坐标下降法的停止准则：当系数变化 < tol 时停止。tol 太大 → 提前停下、欠拟合；tol 太小 → 耗时长 |
| **`selection`** | 坐标选择策略 | `cyclic`（循环遍历所有坐标）vs `random`（随机采样）。random 通常收敛更快但需要设 random_state |

**LassoCV 解决了什么？** LassoCV 内部自动通过 CV 选择最优 α，所以 α 从调参列表中移除。但 **tol 仍然影响 LassoCV 内部的每次拟合** — tol 太大，每步迭代过早停止，导致 α 路径计算不准。

**tol 该如何优化？**

| 策略 | 说明 | 推荐度 |
|---|---|---|
| **固定 tol = 1e-4** | scikit-learn 默认值，在大多数情况下足够 | ★★★★★ (推荐) |
| **Optuna 搜索 tol** | 当前做法，搜索 1e-5~1e-3 | ★★★ (可以保留，但边际收益小) |
| **逐步收紧 tol** | 先用粗 tol 筛选 α，再用精 tol 重算 | ★★ (复杂，不必要) |

**建议**：将 Lasso 的 `tol` 也改为固定值 `1e-4`（scikit-learn 默认），与 Ridge 保持一致，完全跳过 Optuna。因为在小样本（N=88）下，tol 对最终 α 选择和预测性能的影响远小于 α 本身，不值得花计算资源去调。当前保留 Optuna 优化 tol 的做法也行，但边际收益极低。

#### 5.3 ElasticNet 为什么参数最多？

ElasticNet 同时使用 **L1 + L2 混合正则化**：

$$\text{Loss} = \|\mathbf{y} - \mathbf{X}\mathbf{w}\|_2^2 + \alpha \cdot \left[ \rho \cdot \underbrace{\sum |w_i|}_{\text{L1}} + (1-\rho) \cdot \underbrace{\sum w_i^2}_{\text{L2}} \right]$$

因为混合了两种正则化，参数自然更多：

| 参数 | 用途 | 设置建议 |
|---|---|---|
| **`alpha`** | 总正则化强度 | Optuna 搜索 (0.1~100) |
| **`l1_ratio`** (`ρ`) | L1 比例：0=纯 Ridge，1=纯 Lasso | Optuna 搜索 (0.1~0.9) |
| **`max_iter`** | 最大迭代次数 | 设为 50000（比 Lasso 更需要，因为混合目标函数收敛更慢） |
| **`tol`** | 收敛容差 | 设为 1e-4 或 Optuna 搜索 |
| **`selection`** | 坐标选择策略 | `cyclic` 即可 |
| `fit_intercept` | 是否拟合截距 | `True` |
| `precompute` | 预计算 Gram 矩阵 | `True`（小样本下加速） |
| `warm_start` | 热启动 | `True`（在 α 路径上加速） |
| `copy_X` | 是否复制数据 | `True` |

ElasticNet 参数多不是因为它设计得复杂，而是 **L1+L2 混合正则化本身就是最复杂的情况** — 它融合了 Ridge 的稳定性和 Lasso 的稀疏性，代价是需要在 α 和 ρ 两个维度上搜索。当前代码中的 ElasticNet 配置是合理的，不需要修改。

#### 5.4 SVR 的 gamma 参数详解

SVR 使用 RBF (径向基函数) 核：

$$K(\mathbf{x}_i, \mathbf{x}_j) = \exp(-\gamma \cdot \|\mathbf{x}_i - \mathbf{x}_j\|^2)$$

**gamma 的物理含义**：gamma 控制每个训练样本的"影响半径"。

```
gamma 小 (如 0.01):         gamma 大 (如 10):
每个样本影响范围大           每个样本影响范围小
决策边界平滑                 决策边界曲折
 → 高偏差、低方差            → 低偏差、高方差
 → 欠拟合风险                → 过拟合风险
```

**gamma 与数据尺度的关系**：gamma 的最优值与特征尺度密切相关。由于我们使用了 `MinMaxScaler`（特征在 [0,1] 范围内），gamma 的典型最优范围是 0.1~10。

**当前 gamma 范围是否需要调整？**

当前搜索范围：`gamma: 1e-2 ~ 10`（log-uniform）

| 方面 | 评估 |
|---|---|
| 下限 (0.01) | 可能太宽。gamma=0.01 时 RBF 核退化为近似线性核，失去了非线性拟合能力，不太可能在这个范围内找到最优解 |
| 上限 (10) | 合理。gamma=10 时影响半径约为 1/√(2×10) ≈ 0.22，在 MinMaxScaler 后的 [0,1] 空间中刚好能捕捉局部细节 |
| 建议 | 缩小为 **`gamma: 0.1 ~ 10`**（log-uniform），与 C 和 ε 的新范围协调 |

**上次训练 SVR 的 gamma=8.04**，落在 0.1~10 的偏上端，说明数据中有较强的局部模式。这个值在合理范围内，不需要特殊干预。

#### 5.5 SVR 的 C 参数是否需要调整？

**C 的物理含义**：C 控制对训练误差的惩罚。C 越大 → 越不允许误差 → 越容易过拟合。

当前搜索范围：`C: 0.1 ~ 1000`（log-uniform）。上次训练最优 C=73.31，落在中间区域。0.1~1000 的搜索范围合理，**不需要调整**。

#### 5.6 参数修改汇总建议

| 模型 | 参数 | 当前值/范围 | 建议 | 原因 |
|---|---|---|---|---|
| SVR | `epsilon` | 1e-3~0.5 (已改) | 保持 | 已针对活化能预测优化 |
| SVR | `gamma` | 1e-2~10 | **改为 0.1~10** | 0.01下限过宽，MinMaxScaler下无意义 |
| SVR | `C` | 0.1~1000 | 保持 | 范围合理 |
| Ridge | `alpha` | RidgeCV (已改) | 保持 | 自动选择最优 |
| Lasso | `tol` | Optuna 1e-5~1e-3 | **建议改为固定 1e-4** | tol 边际收益极低，不值得 Optuna 搜索；默认值足够 |
| Lasso | `alpha` | LassoCV (已改) | 保持 | 自动选择最优 |
| ElasticNet | 全部 | 现有配置 | 保持 | 配置合理，不需改动 |

**如需执行 gamma 和 Lasso tol 的修改**（这是我基于分析的推荐，请您决策）：

```python
# SVR gamma: 0.1 ~ 10 (原 1e-2 ~ 10)
"gamma": trial.suggest_float("gamma", 0.1, 10, log=True),

# Lasso tol: 固定 1e-4 (原 Optuna 搜索)
"tol": 1e-4,
```

---

## 参考文献与延伸阅读

1. **特征选择**：Guyon, I., & Elisseeff, A. (2003). An introduction to variable and feature selection. *JMLR*, 3, 1157-1182.
2. **集成方法**：Dietterich, T. G. (2000). Ensemble methods in machine learning. *Multiple Classifier Systems*, 1-15.
3. **GPR**：Rasmussen, C. E., & Williams, C. K. I. (2006). *Gaussian Processes for Machine Learning*. MIT Press.
4. **y-randomization**：Rücker, C., Rücker, G., & Meringer, M. (2007). y-Randomization and its variants in QSPR/QSAR. *J. Chem. Inf. Model.*, 47(6), 2345-2357.
5. **外部验证**：Tropsha, A. (2010). Best practices for QSAR model development, validation, and exploitation. *Mol. Inf.*, 29(6-7), 476-488.
6. **Repeated k-fold**：Vanwinckelen, G., & Blockeel, H. (2012). On estimating model accuracy with repeated cross-validation. *BeneLearn*, 39-44.

---

*文档版本: 1.0*
*关联报告: model_analysis_report.md*

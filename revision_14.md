你是一名精通机器学习流程调试与评估的计算科学家。请使用 **superpowers** 技能规划并管理整个诊断与修复任务。过程中可自主调用其他 skills 辅助完成任务。

结合以下评估，判断是否正确，修复其余代码，给出的代码可供参考：

---
你现在的任务是修改本项目代码，请严格按以下步骤完成，并遵循现有代码风格。

## 0. 前期准备（必须做）
- 先通读项目结构，理解 `main.py`、`src/` 中所有模块的代码风格、变量命名、日志写法、模型接口规范。
- 回顾我之前的修改记录或注释中提到的错误（例如缺失 import、参数名写错、类型不匹配等），在本次修改中**全部避免**。

## 1. 注释掉 GPR 模型，暂不启用
- 只需要在 `main.py` 中将 GPR 模型的相关调用、配置、导入注释掉。
- 确保其他代码不会因缺少 GPR 而报错。

## 2. 添加 SISSO 模型
- 参考项目中已有的模型封装方式（如 sklearn 风格的 `fit`、`predict`、`get_params` 等），新建或扩展模型类。
- 你需要：
  a. 如果需要，安装 SISSO 相关依赖（例如 `sisso` 或 `pysisso` 等 Python 接口库），并确认兼容。
  b. 为 SISSO 设置合理的超参数（如特征空间大小、算子集、维度控制、交叉验证折数等），参数名和默认值需写在配置或模型初始化处。
  c. 在训练结束后，**除了保存模型参数、评估指标等统一输出外，还必须额外输出显式解析公式**（即 SISSO 找到的数学表达式），打印到日志并写入结果文件。
  d. 与其他模型保持输出格式一致，方便对比。

## 3. 添加 LightGBM 和 CatBoost 模型
- 同样仿照现有模型封装风格，添加 LightGBM 和 CatBoost。
- 为两者设置一组合理的默认超参数，并在代码注释中说明为何这样选。
- 确保有必要的依赖安装提示或自动检查。
- 输出内容与其他模型对齐。

## 4. 修改日志输出格式
- 当前格式为：`- AI4S_Optimization_models - INFO -`
- 需要改为：`- 模块路径 - INFO -`，例如：
  `- src.train_and_evaluate - INFO -`
  `- src.feature_selection - INFO -`
- 实现方式：检查所有 `logging.getLogger` 的调用，确保每个模块使用 `__name__` 作为 logger 名称（不要用固定字符串 `'AI4S_Optimization_models'`）。同时确认日志配置（formatter）中使用了 `%(name)s` 来输出 logger 名称。
- 修改后，搜索整个项目中是否还有硬编码的 `'AI4S_Optimization_models'`，统一替换。

## 5.（可选）关于进度条的处理
- 当前日志中出现的进度条（如 `0%|          | 0/33 [00:00<?, ?it/s]`）是 tqdm 生成的，通常表示特征选择阶段的迭代进度（33 为子集组合数或交叉验证折数）。
- 请暂时保留该功能，但在进度条出现前用一行日志说明它的含义（英文）
- 同时，在配置中增加一个开关（例如 `show_progress_bar: bool = True`），以便未来一键关闭而不影响代码逻辑。

## 6. 最终检验
- 确保所有新增模型可以在 `main.py` 中正常调用，且不运行的 GPR 不会导致导入或配置错误。
- 运行一次完整的流程（或至少 mock 测试），检查日志格式、SISSO 公式输出、进度条说明是否都符合要求。

#### 任务三
完成以上所有分析和代码修改后，创建一个新的 Markdown 文件 **`debug_log_and_revisions_13.md`**，内容需包括：
1. **问题修正**：针对上述给出的问题，逐个思考修正，列出所有详细修正思路、修正代码、修正后的效果.
2. **修正后的代码使用指导**：如有命令行参数变化、配置文件字段变化等，说明如何运行新版本。
3. **遗留问题与建议**：如果在修复过程中仍存在无法完全解决的疑虑，或对未来运行的建议，一并列出。
4. 根据新代码，更新README、Pipeline.md、CLAUDE.md、user_manual.md等相关说明文档

#### 任务四
- 汇总全部修改的文件列表。
- 确认所有修改已保存，导入和语法无误（至少静态检查关键脚本）。
- 将总结告知用户，指明输出的核心文档为 `debug_log_and_revisions_13.md`。

---
**注意**：
- 所有修改均需基于对实际输出文件的完整阅读，不可臆测。
- 若某疑惑需要进一步运行小规模实验验证，可创建临时脚本并说明。
- 确保所有代码修改有详细**英文注释**，解释为何如此修改。